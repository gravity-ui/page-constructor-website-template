// #!if BUILD_MODE === "default"
export {getServerSideProps} from '../../server/components/sitemap/server';
// #!endif

// Default export to prevent next.js errors
export default function Sitemap() {}
